{
  "name": "cypress-nopcommerce",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": { 
  "cypress:run": "npx cypress run --env ENV=dev && npx cypress run --env ENV=staging && npx cypress run --env ENV=qa && npx cypress run --env ENV=production",
  "cypress:open": "npx cypress open --env allure=true,ENV=qa",
  "allure:report": "allure generate allure-results --clean -o allure-report",
  "allure:open": "allure open allure-report",
  "test:allure": "npm run cypress:run && npm run allure:report && npm run allure:open"
},
    
  "author": "Akan Antia",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@shelex/cypress-allure-plugin": "^2.40.2",
    "allure-commandline": "^2.32.0",
    "cross-env": "^7.0.3",
    "cypress": "^13.15.2",
    "rimraf": "^6.0.1"
  }
}
